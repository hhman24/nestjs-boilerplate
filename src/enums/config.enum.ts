export enum ConfigKeyEnum {
    APP = "app",
    DB = "db",
    MY_SQL = "my_sql"
}

export enum EnvironmentEnum {
    DEVELOPMENT = "development",
    STAGING = "staging",
    PRODUCTION = "production",
    TESTING = "testing"
}
